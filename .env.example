# Copy file này thành .env và điền giá trị (KHÔNG commit .env chứa secret)

SECRET_KEY=change-me-to-a-long-random-string
# Khoá bí mật dùng để ký JWT. Sinh ngẫu nhiên bằng secrets.token_urlsafe(32)

DATABASE_URL=sqlite:///./test.db
# Chuỗi kết nối DB. Ví dụ SQLite local: sqlite:///./test.db

ALGORITHM=HS256
# Thuật toán ký JWT (ví dụ: HS256)

ACCESS_TOKEN_EXPIRE_MINUTES=30
# Thời gian sống (phút) của access token

APP_NAME=BlogAPI
# Tên ứng dụng (hiển thị logs/docs)

ENV=development
# Môi trường chạy: development/test/production
